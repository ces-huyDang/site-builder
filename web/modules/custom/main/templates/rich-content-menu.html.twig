{% for item in data %}
  <main id={{ "content-block-" ~ loop.index }} class="card position-absolute z-3 rounded-0 col-8 rich-content-block d-none">
    <section class="card-body ctgy-menu">
      {% for term in item %}
        <div class="ctgy-menu-item">
          <a class="ctgy-link" href={{ "/taxonomy/term/" ~ term['tid'] }}>
            {{ (term['term_name']) }}</a>
        </div>
      {% endfor %}
    </section>
  </main>
{% endfor %}
