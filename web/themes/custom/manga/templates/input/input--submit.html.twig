{#
/**
 * @file
 * Default theme implementation for an 'input' #type form element.
 *
 * Available variables:
 * - attributes: A list of HTML attributes for the input element.
 * - children: Optional additional rendered elements.
 *
 * @see template_preprocess_input()
 *
 * @ingroup themeable
 */
#}

{% if path('<current>') starts with '/manga' or path('<current>') starts with '/home-page' %}
  <button class="search-btn" {{ attributes.removeAttribute('value') }}>
    <i class="fa fa-search text-orange"></i>
  </buton>
{% else %}
  <input class="manga-btn" {{attributes}}/>{{ children }}
{% endif %}
